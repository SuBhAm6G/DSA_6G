#include <stdio.h>
#include <stdlib.h>
//Singly linked list
struct node{
    int val;
    struct node* next;
};

//Doubly Linked list
struct node{
    struct node* prev;
    int val;
    struct node* next;
};

int main() {
    
    return 0;
}